# Migración WordPress → GitHub Pages: pendiente

La migración a Hugo + Stack ya está en producción en
https://unaqaenapuros.github.io/ (contenido migrado, analíticas,
publicación programada, auto-publicación en LinkedIn vía Make y
notificación por email, todo funcionando). WordPress ya no publica
nada nuevo. Este fichero se borrará en cuanto quede lo de abajo
resuelto.

## Pendiente

- [ ] **Dominio propio** (opcional): si quieres usar un dominio
      distinto a `unaqaenapuros.github.io`, hay que configurar
      `CNAME` y `baseURL` en [hugo.toml](hugo.toml).

## Decidido

- Comentarios de WordPress: **no se migran**, se quedan fuera.
